# Audit santrionline.com — 2026-08 (Fase 0, read-only)

> Repo: `Santrionlinecom/web` @ `b9547a4` (main)
> Metode: pembacaan statis seluruh `src/`, konfigurasi, dan hasil build nyata.
> Tidak ada satu pun file kode yang diubah pada fase ini.

## 1. Stack terverifikasi

| Komponen | Versi | Catatan |
|---|---|---|
| Svelte | **5.55.2, runes mode dipaksa** via `svelte.config.js` (`runes: true` kecuali node_modules) | Sudah `$props()`/`$state()`/`$effect()` |
| SvelteKit | 2.57.0 | |
| Tailwind | **4.2.2, CSS-first** (`@theme` di `src/routes/layout.css`) | **Tidak ada `tailwind.config.js`** — instruksi fase 2a harus diadaptasi ke `@theme` |
| Adapter | @sveltejs/adapter-cloudflare 7.2.8 | Pages, output `.svelte-kit/cloudflare` |
| Auth | **Lucia TIDAK ada di repo ini** | Auth hanya di app.santrionline.com. Di sini cuma gerbang admin invoice buatan sendiri |
| Deps runtime | `dependencies: {}` — nol | Semua devDeps terpakai; **tidak ada dependency untuk dihapus** |

## 2. Peta rute & komponen

```
src/routes/
├── +layout.svelte            (12 baris — favicon + render children saja)
├── +page.svelte              (887 baris — SEMUA landing page dalam 1 file)
├── layout.css                (334 baris — @theme + seluruh motion CSS)
├── literasi/apa-itu-santri-online/+page.svelte  (79 baris, konten statis)
├── invoice/[kode]/           (+page.server.ts + svelte — tagihan publik, noindex ✓)
├── invoice/admin/            (+page.svelte 300+ baris — panel admin)
└── api/
    ├── chat/+server.ts       (AI chat Groq, guard fatwa — sehat)
    ├── admin/invoice/+server.ts  (CRUD invoice, Bearer INVOICE_ADMIN_SECRET)
    └── midtrans/webhook/+server.ts  (webhook pembayaran)

src/lib/
├── components/jasa/FaqItem.svelte      ← MATI (0 pemakai)
├── components/jasa/PricingCard.svelte  ← MATI (0 pemakai)
├── index.ts                            ← placeholder kosong
├── server/invoice.ts                   (tipe + util invoice, dipakai)
└── assets/ (favicon, logo)
```

## 3. Daftar temuan

### a) Runtime / keamanan

| # | Temuan | Bukti | Tingkat |
|---|---|---|---|
| A1 | **Hash SHA-256 password admin tertanam di bundle publik.** Siapa pun bisa `view-source` dan brute-force offline. Pertahanan nyata memang di server (`Bearer INVOICE_ADMIN_SECRET`, `api/admin/invoice/+server.ts:62-67`), jadi gerbang klien ini dekoratif — tapi mengiklankan hash itu tetap buruk. | `invoice/admin/+page.svelte:35-36` | SEDANG — **laporan dulu sesuai aturan #5**, sentuh hanya dengan izin |
| A2 | Webhook Midtrans masih hidup padahal commit `b9547a4` menyatakan "pembayaran hanya di app". Kode sehat, tapi jadi permukaan serangan + kebingungan arah. | `api/midtrans/webhook/+server.ts` (seluruh file) | RENDAH — **butuh keputusan Mas: matikan atau biarkan** (aturan #5, area pembayaran) |

### b) Layout / responsif

| # | Temuan | Bukti |
|---|---|---|
| B1 | Google Fonts dimuat via `@import url()` **di dalam CSS** — render-blocking berantai (CSS → font CSS → font), memperlambat LCP terutama di HP. Harusnya `<link rel="preconnect">` + `<link rel="stylesheet">` di `app.html` | `layout.css:1` |
| B2 | Bottom-nav `fixed` mobile dikompensasi `pb-28` di footer — benar; tapi section `#tanya` (chat AI) tidak punya padding-bottom serupa saat keyboard HP terbuka; risiko input tertutup nav. Perlu uji perangkat nyata | `+page.svelte:807,865` |
| B3 | Tidak ditemukan overflow horizontal statis; grid memakai `minmax(0,1fr)` dengan benar. Perlu verifikasi visual 360px saat Fase 4 | — |

### c) Aksesibilitas

| # | Temuan | Bukti |
|---|---|---|
| C1 | Semua 4 `<img>` ber-`alt` ✓, 1×`<h1>` ✓, 15 `aria-label` ✓, `focus-visible` hanya di 4 tempat — tombol/link lain belum konsisten | `+page.svelte` |
| C2 | **Tidak ada skip-link** ke konten utama (hanya 1 kecocokan `sr-only`, bukan skip-link) | `+page.svelte` |
| C3 | `prefers-reduced-motion: reduce` SUDAH ada dan mematikan hero/reveal/typing ✓ — tinggal dipertahankan saat Fase 2 | `layout.css:306-330` |
| C4 | Heading order aman (h1→h2→h3), tidak ada lompatan | scan `<h[1-6]` |

### d) SEO

| # | Temuan | Bukti |
|---|---|---|
| D1 | Halaman literasi (halaman kunci kata kunci "santri online") **tanpa hreflang, tanpa JSON-LD Article, tanpa meta geo** — padahal beranda lengkap | `literasi/.../+page.svelte` (hanya canonical+og dasar) |
| D2 | `sitemap.xml` statis, `lastmod` beku di `2026-08-09` (2 entri) — bohong ke crawler setelah deploy kemarin | `static/sitemap.xml` |
| D3 | Invoice publik & admin `noindex` ✓ benar | `invoice/*` |
| D4 | Beranda: title/desc/OG/canonical/geo/JSON-LD lengkap ✓ (hasil kerja kemarin) | `+page.svelte:346-390` |

### e) Kode mati / kebersihan

| # | Temuan | Bukti |
|---|---|---|
| E1 | `FaqItem.svelte` + `PricingCard.svelte` — **nol pemakai** (sisa era "jasa") | `grep -rn` di seluruh routes: 0 hasil |
| E2 | `src/lib/index.ts` placeholder kosong | isi 1 baris komentar |
| E3 | `+page.svelte` 887 baris memuat: SEO, struktur data, chat AI (state+fetch), modal, drawer mobile, IntersectionObserver, dan seluruh markup 9 section — kandidat utama refactor Fase 3 | `wc -l` |
| E4 | Tidak ada `: any`, `@ts-ignore`, atau `console.log` tertinggal ✓ | grep bersih |
| E5 | Tidak ada pemakaian `svelte/transition`, `svelte/easing`, `onNavigate`, atau View Transitions — Fase 2 mulai dari nol yang bersih | grep: 0 hasil |
| E6 | Tidak ada skeleton/shimmer untuk state loading chat | grep: 0 hasil |

## 4. Baseline performa (build nyata, sebelum perubahan apa pun)

```
total _app (immutable): 292 KB
nodes/2 (beranda):       47.0 KB   ← terbesar, wajar (887 baris satu file)
nodes/4 (invoice/admin): 16.6 KB
nodes/3 (literasi):       8.2 KB
nodes/5 (invoice/kode):   6.7 KB
chunk terbesar:          46.7 KB + 31.3 KB (runtime Svelte + kit)
```

`npm run build` ✓ dan `npm run check` ✓ (0 error 0 warning) pada baseline.

## 5. Penyesuaian rencana yang perlu Mas ketahui

1. **Fase 2a token via `@theme` di `layout.css`**, bukan `tailwind.config.js` — proyek ini Tailwind v4 CSS-first. Semangatnya sama (token terpusat), lokasinya beda.
2. **Aturan #5 (jangan sentuh auth/pembayaran)**: temuan A1 (hash admin di klien) dan A2 (webhook Midtrans yatim) berada persis di area itu. Saya TIDAK akan menyentuhnya tanpa keputusan eksplisit.
3. Repo ini TIDAK memakai Lucia — brief menyebutnya, kenyataannya auth ada di app.santrionline.com. Tidak ada dampak, hanya pelurusan fakta.
4. Tidak ada dependency yang bisa/perlu dihapus — `dependencies` kosong total.

## 6. Usulan urutan Fase 1 (menunggu persetujuan)

| Prioritas | Bug | Alasan |
|---|---|---|
| 1 | B1 — font render-blocking | Satu-satunya yang menyentuh LCP semua halaman |
| 2 | D1 — SEO halaman literasi | Halaman kunci brand "santri online" |
| 3 | D2 — sitemap lastmod | 2 menit, jujur ke crawler |
| 4 | C2 — skip-link + C1 focus-visible konsisten | A11y murah |
| 5 | E1+E2 — hapus kode mati | Bersih sebelum Fase 2 |
| (izin) | A1 — hapus gerbang klien dekoratif ATAU pindah ke server penuh | Butuh keputusan |
| (izin) | A2 — matikan webhook Midtrans di web | Butuh keputusan |
